# Example environment configuration for using Vault/OpenBao
# Copy this to .env and adjust values as needed

# ===========================
# Vault Configuration
# ===========================
VAULT__ENABLED=true
VAULT__URL=http://localhost:8200
VAULT__TOKEN=your-vault-token-here
# VAULT__NAMESPACE=optional-namespace  # For Vault Enterprise
VAULT__MOUNT_PATH=secret
VAULT__KV_VERSION=2

# ===========================
# Application Settings
# ===========================
ENVIRONMENT=development
APP_NAME=dotmac-platform
APP_VERSION=1.0.0

# ===========================
# Non-sensitive configuration
# ===========================
DATABASE__HOST=localhost
DATABASE__PORT=5432
DATABASE__DATABASE=dotmac

REDIS__HOST=localhost
REDIS__PORT=6379

# ===========================
# Sensitive values (loaded from Vault)
# ===========================
# The following values will be loaded from Vault at startup:
# - SECRET_KEY (from vault: app/secret_key)
# - DATABASE__PASSWORD (from vault: database/password)
# - REDIS__PASSWORD (from vault: redis/password)
# - JWT__SECRET_KEY (from vault: auth/jwt_secret)
# - EMAIL__SMTP_PASSWORD (from vault: smtp/password)
# - STORAGE__ACCESS_KEY (from vault: storage/access_key)
# - STORAGE__SECRET_KEY (from vault: storage/secret_key)

# You can still provide fallback values here for development:
SECRET_KEY=dev-only-secret-key
DATABASE__PASSWORD=dev-password
JWT__SECRET_KEY=dev-jwt-key