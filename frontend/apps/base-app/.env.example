# API Configuration
# IMPORTANT: Use NEXT_PUBLIC_API_BASE_URL (not NEXT_PUBLIC_API_URL)
# All API calls now use the centralized config from lib/config.ts
# Note: No /api suffix - Next.js proxy handles routing
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws

# Observability - Connect to backend OpenTelemetry Collector
NEXT_PUBLIC_OTEL_ENDPOINT=http://localhost:4318

# Environment
NEXT_PUBLIC_ENVIRONMENT=development

# Optional: Analytics (if using)
# NEXT_PUBLIC_ANALYTICS_ID=your-analytics-id

# Optional: Public Site URL
# NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Development Notes:
# - Client-side requests use relative URLs (empty string) that go through Next.js proxy
# - Server-side requests use the full NEXT_PUBLIC_API_BASE_URL
# - This ensures cookies work correctly and avoids CORS issues
