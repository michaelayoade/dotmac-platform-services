query SecretsQuery($pathPrefix: String) {
  secrets(pathPrefix: $pathPrefix) {
    path
    version
    createdAt
    updatedAt
    tags
    description
  }
}

query SecretHistoryQuery($path: String!) {
  secretHistory(path: $path) {
    version
    createdAt
    createdBy
    description
  }
}