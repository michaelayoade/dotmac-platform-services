# DotMac Control Workers Environment Configuration
# Copy this file to /etc/dotmac/control-workers.env and update with actual credentials

# Database connection URL
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://dotmac_user:your_secure_password_here@localhost:5432/dotmac

# Redis connection URL
# Format: redis://[:password@]host:port/db
# Note: If Redis has no password, omit the `:password@` part
REDIS_URL=redis://localhost:6379/0

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Python path (usually not needed to change)
PYTHONPATH=/opt/dotmac/src
