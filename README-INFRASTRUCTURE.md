# Infrastructure Guide

The infrastructure reference now lives at `docs/architecture/INFRASTRUCTURE.md` to keep all production docs in one place.

- Start shared services: `docker compose -f docker-compose.infra.yml up -d`
- Full context and port map: see `docs/architecture/INFRASTRUCTURE.md`
